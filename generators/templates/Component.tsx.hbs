"use client";

import { useState } from "react";
type {{ pascalCase name }}Props = React.ComponentProps<"button">;
export function {{ pascalCase name }}(props: {{ pascalCase name }}Props) {
  const [count, setCount] = useState(0);
  const handleIncreaseCount = () => setCount((previous) => previous + 1);
  return (
    <button
      className="my-7 flex rounded-full bg-red-800 px-4 py-2 text-red-50 transition-colors hover:bg-red-900"
      onClick={handleIncreaseCount}
      {...props}
    >
      You clicked this button this many times: {count}
    </button>
  );
}
